import 'package:flutter/material.dart';

class TextStyled extends StatelessWidget {
  const TextStyled({super.key});

  @override
  Widget build(context) {
    return const Text('lol', style: TextStyle(
      color: Colors.black54,
    ));
  }
}
